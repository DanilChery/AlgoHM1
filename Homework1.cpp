#include <iostream>
#include <fstream>
#include <string>
using namespace std;
int main()
{
    string snums;
    cin >> snums;
   // int nums = stoi(snums);
   // cout << nums;
    cout << snums.length();

}

